import sys

def twos(s):
    splits = []
    for i in range(0,len(s)-1):
        two = s[i:i+2]
        splits.append(two)
    print(splits)
    c = dict()
    for i in range(1, len(splits)):
        try:
            if splits[i] == splits[i-1]: continue
            c.setdefault(splits[i], 0)
            c[splits[i]] += 1
            if c[splits[i]] > 1: return True
        except:
            pass
    return False

def threes(s):
    splits = []
    for i in range(0,len(s)-2):
        three = s[i:i+3]
        splits.append(three)
    print(splits)
    for i in range(0, len(splits)):
        if splits[i][0] == splits[i][2]: return True
    return False

nice = []
with open(sys.argv[1]) as f:
    for line in f:
        s = line.strip()
        print(s)
        t = [ s, twos(s), threes(s) ]
        print(t)

